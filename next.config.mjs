/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.gstatic.com', 'utfs.io', 'img.clerk.com','2oie3zggv0.ufs.sh'],
      },
};

export default nextConfig;
